# secrets, never commit
.env
.env.local

# run outputs (samples/ is committed deliberately, runs/ is not)
runs/
*.pyc
__pycache__/
.streamlit/secrets.toml

# os
.DS_Store
Thumbs.db

# capture log holds real email addresses, never commit it
captures.csv

# streamlit secrets, never commit
.streamlit/secrets.toml

# feedback log, holds no PII but is per-deployment
feedback.csv

reviews.csv
